# macOS
.DS_Store

# Working directories for campaign production, not part of the library site.
#
# html5/ and documents/ are deliberately tracked and published via GitHub Pages:
# the ad previews are linked from the strategy documents, and the documents
# themselves are served so Heyzine can fetch them for flipbook conversion.
# Both are world-readable — keep client-internal material out of them.
properties/
reports/
aperturereportbot/
